const input = {
  bgcolor: "background.default",
  borderRadius: "15px",
  p: 2
}

const icon = {
  mr: 2,
}

/**
 * Styles for inputs
 */
const inputStyles = {
  input,
  icon,
}

export default inputStyles

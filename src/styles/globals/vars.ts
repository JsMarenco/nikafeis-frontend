const centeredElements = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
}

const skeletonAnimation: "pulse" | "wave" = "wave"

/**
 * Styles variables
 */
const stylesVars = {
  centeredElements,
  skeletonAnimation
}

export default stylesVars
